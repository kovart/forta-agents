export type AssetMetadata = {
  symbol?: string
  address: string,
  amount: string,
  price: string,
  total: string
};

export type TokenConfig = {
  symbol: string;
  address: string;
  decimals: number
};
